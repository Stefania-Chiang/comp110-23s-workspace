"""Practicing for ... in ... loops."""

def sum(xs: list[float]) -> float:
    """return sum off all elements in xs"""
    sum_total: float = 0.0
    idx: int = 0

    for sum_total in xs:
        return sum_total
